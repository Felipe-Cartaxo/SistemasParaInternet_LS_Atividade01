// Problema: Monte um programa para calcular a área de um círculo

function calc_area_circulo (raio) {
	const pi = 3.14159265359

	return area = (pi * (raio**2))
}

// Saída do programa
console.log(calc_area_circulo(10))
console.log(calc_area_circulo(1))
console.log(calc_area_circulo(0))